import 'package:flutter/material.dart';

class AboutUs extends StatelessWidget {
  const AboutUs({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('We are wanting to make the best use of our app to moniter your health and keep a database of everything ')) ;
  }
}